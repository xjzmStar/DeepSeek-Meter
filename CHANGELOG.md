# 更新日志

## v3.0.0-snapshots-1 (2026-08-22)
### 变更
- 更新检查改为浏览器跳转 GitHub Release 页面下载（移除内置下载器）
  - 发现新版本后弹窗显示更新日志，确认后打开默认浏览器跳转 Release 页
  - 删除 `download_and_install`、进度条窗口、VBS 自动替换脚本
  - GitHub 下载速度远快于内置 urllib 下载

### 新增
- 挂件模式
  - 无标题栏、半透明显示（30%~100% 可调）
  - 四角定位（左上/右上/左下/右下）
  - 鼠标穿透（可选）
  - 双击挂件回到窗口模式
  - 挂件字体颜色自定义（时间/日期分别选色）
- 全局快捷键
  - `Ctrl+Alt+D` 显示/隐藏
  - `Ctrl+Alt+S` 打开设置
  - `Ctrl+Alt+M` 切换窗口/挂件模式
  - `Ctrl+Alt+Q` 退出程序
  - 快捷键可在设置面板自定义
- 窗口大小跟随字号自动缩放
- 多屏位置记忆（支持副屏坐标保存与恢复）

## v2.1.0 (2026-08-21)
### 新增
- 字体自定义功能
  - 设置面板新增「字体」选项卡
  - 字体样式：支持切换微软雅黑、思源黑体、等线、楷体、Consolas 等 9 种预设字体
  - 字体大小：滑块实时调节（0.6x ~ 1.8x），预览即时反馈
  - 设置保存后全局生效（时间、余额、日期等全部文字同步刷新）
  - 字体设置跟随主题，深色/浅色模式下预览配色自动适配

## v2.0.4 (2026-08-21)
### 新增
- 应用 logo 全面更新为鲸鱼仪表盘风格（深蓝底 + 蓝色鲸鱼 + 仪表盘 + 太阳月亮）
- 托盘图标使用新 logo
### 修复
- 修复 `get_data_path` 未定义导致的启动崩溃
  - PyInstaller onefile 模式下需要从 `sys._MEIPASS` 获取数据文件路径
- 将 `app_logo.png` 打包进 exe，确保新图标正常加载

## v2.0.3 (2026-08-21)
### 修复
- 修复自动更新替换 exe 路径指向临时目录的 bug
  - PyInstaller onefile 模式下，`sys.executable` 指向临时解压目录
  - 自动更新下载新版 exe 后，VBS 脚本尝试替换的是临时目录下的 exe，而非用户实际运行的 exe
  - 改用 `sys.argv[0]` 获取真实 exe 路径，确保更新后能正常重启安装

## v2.0.2 (2026-08-21)
### 修复
- 修复开机自启脚本指向 PyInstaller 临时目录的 bug
  - `sys.executable` 指向临时解压目录，导致生成的开机自启 VBS 脚本路径错误
  - 改用 `sys.argv[0]` 获取用户实际运行的 exe 路径
  - VBS 脚本简化，不再传递临时目录的 app.py 参数

## v2.0.1 (2026-08-20)
### 新增
- 余额颜色增加欠费红色档位
  - 余额 ≤ 0 时显示红色 `#FF4444`
  - 低余额从黄色改为橙色 `#FF9800`，与阈值联动
  - 阈值不再写死 5，改为读取设置面板中的 `low_balance_threshold`（默认 2.0）

## v2.0.0 (2026-08-20)
### 新增
- 独立应用版（CustomTkinter + PyInstaller）
  - 桌面悬浮窗：实时时钟 + DeepSeek 余额 + 峰谷时段
  - 系统托盘图标：右键菜单（显示/设置/检查更新/退出）
  - 明暗主题切换（dark / light / 跟随系统）
  - 窗口置顶可选
  - 低余额提醒
  - 开机自启动
  - 拖拽调窗口大小
  - 自动更新：启动时静默检查 GitHub 新版本，发现新版自动下载替换
- Linux 版构建支持（GitHub Actions 自动打包）
- 仓库结构调整：Rainmeter 版移入 `rainmeter/`，独立版放入 `app/`

## v1.0.1 (2026-08-20)
### 修复
- README 安装步骤：git clone 改为 Release 下载

## v1.0.0 (2026-08-20)
### 新增
- 初始发布：Rainmeter 版
- 实时时钟 + DeepSeek 余额监控 + 峰谷电价时段
- 后台静默运行 + 开机自启动
